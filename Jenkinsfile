pipeline{
    agent any
    stages{
        stage("THIS WILL PRINT BUILD NUMBER AND BUILD ID"){
            steps{
                echo "$BUILD_NUMBER"
                echo "$BUILD_ID"
            }
        }
    }
}