pipeline{
    agent any
    stages{
        stage{
            steps("THIS WILL PRINT BUILD NUMBER AND BUILD ID"){
                echo "$BUILD_NUMBER"
                echo "$BUILD_ID"
            }
        }
    }
}